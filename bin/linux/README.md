# MelT v1.0.0 — Linux Binary

Standalone binary for x86_64 Linux. ffmpeg bundled.

## Usage

chmod +x melt
./melt --version
./melt          # interactive mode
./melt --help   # full options

## Build

Built with PyInstaller 6.20.0 on Kali Linux (WSL2).
