# MelT v1.0.0 — Windows Binary

Standalone .exe for x86_64 Windows. ffmpeg bundled. Custom icon.

## Usage

melt.exe --version
melt.exe          # interactive mode
melt.exe --help   # full options

## Build

Built with PyInstaller 6.20.0 on Windows 10.
